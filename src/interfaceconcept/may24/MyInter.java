package interfaceconcept.may24;

public interface MyInter {
	
	//Constant should not be changed by anyone e.g. your overdraft is £100

	// public static final double PI = 3.14;
	double PI = 3.14;// by default interfaces have fields i.e.  public static final
					 // Static Final, check the left pane and display it in project exp
	
	void display (int x);//abstract method has no body (by default it's abstract)
	
	
}
